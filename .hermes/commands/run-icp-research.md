# run-icp-research

Run the `client-intake` skill first if `intake-summary.md` is missing.

Then run the `icp-research` skill.

Input:

```text
clients/<client>/inputs/
```

Output:

```text
clients/<client>/outputs/icp-brief.md
```

